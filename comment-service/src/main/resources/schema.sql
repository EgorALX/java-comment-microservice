CREATE TABLE IF NOT EXCIST comments (
    comment_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id INTEGER NOT NULL,
    news_id INTEGER NOT NULL,
    description VARCHAR(256) NOT NULL
);